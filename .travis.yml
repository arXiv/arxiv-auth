language: python
sudo: required
services:
  - docker
cache: pip
env:
  - MIN_SCORE=9
os:
  - linux
python:
  - "3.6"
script:
  - pip install pipenv
  - pipenv install --dev
  - pipenv install ./users
  - ./lintstats.sh
  - ./tests/docker_tests.sh
  - pipenv run nose2
after_success:
  - coveralls
