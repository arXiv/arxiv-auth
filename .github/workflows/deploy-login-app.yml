name: Deploy arxiv-auth login app

# Set custom run name to show project name in workflow runs list
run-name: "Deploy Login App ${{ inputs.env }} ${{ inputs.image_tag }}"

on:
  workflow_call:
    inputs:
      env:
        description: "Environment"
        required: true
        type: string
        default: ""
      image_tag:
        description: "Docker image tag (latest or the commit SHA)"
        required: false
        type: string
        default: "latest"
      image_name:
        description: "Docker image name"
        required: false
        type: string
        default: "login-app"

  workflow_dispatch:
    inputs:
      env:
        description: "Environment"
        required: true
        type: environment
        default: ""
      image_tag:
        description: "Docker image tag (latest or the commit SHA)"
        required: false
        type: string
        default: "latest"
      image_name:
        description: "Docker image name"
        required: false
        type: string
        default: "login-app"

permissions:
  contents: read
  pull-requests: write
  id-token: write

env:
  PORT: "8000"

jobs:
  format-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up terraform
        uses: hashicorp/setup-terraform@v3
      - name: Check version
        run: terraform --version
      - name: Format check
        run: terraform fmt -check terraform

  deploy:
    runs-on: ubuntu-latest
    needs: format-check
    environment: ${{ inputs.env }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate with target project workload identity
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: projects/${{ vars.PROJECT_NUMBER }}/locations/global/workloadIdentityPools/github/providers/github-actions-provider
          service_account: github-actions-sa@${{ vars.PROJECT_NAME }}.iam.gserviceaccount.com
          project_id: ${{ vars.PROJECT_NAME }}

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Set up terraform
        uses: hashicorp/setup-terraform@v3

      - name: Check version
        run: terraform --version

      - name: Set environment variables
        description: Setup which command line args for tf for tfvars file to use and the GCR image
        run: |
          TERRAFORM_ARGS="-var-file=\"envs/${{ vars.ENV_VAR }}.tfvars\" \
            -var=\"image_path=gcr.io/arxiv-development/${{inputs.image_name}}:${{inputs.image_tag}}\""

          echo "TERRAFORM_ARGS=$TERRAFORM_ARGS" >> $GITHUB_ENV
        working-directory: terraform

      - name: Initialize terraform state in a GS bucket
        run: terraform init -backend-config="bucket=${{ vars.TERRAFORM_BUCKET }}"
        working-directory: terraform

      - name: Terraform Plan
        run: terraform plan ${{ env.TERRAFORM_ARGS }} -out=tfplan

      - name: Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-${{ inputs.env }}
          path: tfplan
          retention-days: 30

      - name: Apply Terraform
        run: terraform apply ${{ env.TERRAFORM_ARGS }} -auto-approve
        working-directory: terraform

      # # Remove this once services/URLs are consolidated
      # - name: Deploy Legacy Backend (Manual Copy)
      #   run: |
      #     gcloud run deploy student-activity-dashboard \
      #       --image gcr.io/arxiv-development/${{inputs.image_name}}:${{inputs.image_tag}} \
      #       --region us-central1 \
      #       --port ${{ env.PORT }}
